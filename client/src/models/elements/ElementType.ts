/**
 * Enum representing the different types of UI elements
 */
export enum ElementType {
  TEXT = "text",
  INPUT = "input",
  DROPDOWN = "dropdown",
  BOOLEAN = "boolean",
  STAR_RATING = "star_rating",
}

/**
 * Enum representing the different types of input fields
 */
export enum InputType {
  TEXT = "text",
  NUMBER = "number",
  DATE = "date",
  BOOLEAN = "boolean",
}
